# Ассоциативный массив
internal = []

# Вводим в исходный массив одно значение
data = {}
data["key"] = "value"
#print(data["key"])

for i in range(10):
    print(abs(hash('key')))

# index = abs(hash('key')) % 1000
# print(hash('key'))
# print(abs(hash('key')))
# print(index)

